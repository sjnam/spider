# Binaries (the compiled command; note: do NOT use /spider, it would also
# ignore the spider/ package directory)
*.exe
*.test
*.out

# Go workspace / build caches
go.work
go.work.sum

# Editor / OS
.DS_Store
.idea/
.vscode/
